import React from 'react';

const ContentFeed = ({data}) => {
    return (
        <div>
            // search / search + button
            // ContentGrid
        </div>
    );
};

export default ContentFeed;